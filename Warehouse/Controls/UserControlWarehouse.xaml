<UserControl x:Class="Warehouse.Controls.UserControlWarehouse"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Warehouse.Controls"
             
             xmlns:local2="clr-namespace:Warehouse"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="999">

    <!--xmlns:local1="clr-namespace:Warehouse.Controls.Helpers"-->

    <UserControl.Resources>
        <DataTemplate x:Key="listTemplate">
            <StackPanel Margin="4">
                <TextBlock FontFamily="Verdana" FontSize="14" Text="{Binding Path=WhName}" HorizontalAlignment="Left" />
            </StackPanel>
        </DataTemplate>
        <!--<local1:PriceConverter x:Key="PriceConv"/>
        <locall:Discount BackgroundConverter x:Key="BgConv" />-->
    </UserControl.Resources>
    <UserControl.CommandBindings>
        <CommandBinding Command="local2:WindowCommands.WhAdd" Executed="WhAdd_Executed"/>
        <CommandBinding Command="local2:WindowCommands.WhEdit" Executed="WhEdit_Executed"/>
        <CommandBinding Command="local2:WindowCommands.WhDelete" Executed="WhDelete_Executed"/>
    </UserControl.CommandBindings>



    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="lbxWhList" Grid.Column="0"
                 ItemTemplate="{StaticResource listTemplate}" SelectionChanged="lbxWhList_SelectionChanged">
            <!---->
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Hoвый склaд" Command="local2:WindowCommands.WhAdd" />
                    <MenuItem Header="Изменить склад" Command="local2:WindowCommands.WhEdit" />
                    <MenuItem Header="Удалить склад" Command="local2:WindowCommands.WhDelete" />
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>
        <GridSplitter Grid.Column="1" ShowsPreview="False" Width="3" 
                      HorizontalAlignment="Center" VerticalAlignment="Stretch" />
        <DockPanel Grid.Column="2">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>



                <!-- Панель управления -->
                <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                    <TextBlock Text="Пoиck: " VerticalAlignment="Center" Margin="0 0 5 0"/>
                    <TextBox x:Name="TxtSearch" Width="250" TextChanged="TxtSearch_TextChanged" Margin="0 0 0 10"/>
                    <!--<TextBlock Text="Поставшик:" VerticalAlignment="Center" Margin="0 0 5 0"/>
                    <ComboBox x:Name="CmbSupplier" Width="150" SelectionChanged="CmbSupplier_SelectionChanged"/>
                    <TextBlock Text="Cортировка:" VerticalAlignment="Center" Margin="0 0 5 0"/>
                    <ComboBox x:Name="CmbSort" Width="120" SelectionChanged="CmbSort_SelectionChanged">
                        <ComboBoxItem Content="Без сортировки"/>
                        <ComboBoxItem Content="OcтатоK 1"/>
                        <ComboBoxItem Content="OCTатOK 1"/>
                    </ComboBox>
                    <Button x:Name="BtnAdd" Content="Добавить товар" Visibility="Collapsed" Click="BtnAdd_Click"/>-->
                </StackPanel>



                <!-- DataGrid -->
                <DataGrid x:Name="DgProducts"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          SelectionMode="Single"
          Grid.Row="1"
          SelectionChanged="DgProducts_SelectionChanged"
          MouseDoubleClick="DgProducts_MouseDoubleClick"
          Margin="5"
          HeadersVisibility="Column"
          RowHeight="30"
          CanUserResizeColumns="True">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="Наименование"
                            Binding="{Binding Name}"
                            Width="2*" />

                        <DataGridTextColumn Header="Категория"
                            Binding="{Binding Category.Name}"
                            Width="120" />

                        <DataGridTextColumn Header="Производитель"
                            Binding="{Binding Manufacturer.Name}"
                            Width="140" />

                        <DataGridTextColumn Header="Поставщик"
                            Binding="{Binding Supplier.Name}"
                            Width="120" />

                        <DataGridTextColumn Header="Цена"
                            Binding="{Binding Price, StringFormat={}{0:F2}}"
                            Width="90" />

                        <DataGridTextColumn Header="Остаток"
                            Binding="{Binding StockQuantity}"
                            Width="80" />

                        <DataGridTextColumn Header="Скидка %"
                            Binding="{Binding DiscountPercent}"
                            Width="80" />

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
